# 批签发证明拆分工具 — 产品规划

## 一、产品定位

面向基层疾控中心免疫规划人员的**批签发证明文件管理工具**。

用户无需具备任何编程能力，下载安装包后双击启动，通过浏览器完成全部操作。

---

## 二、目标用户

| 角色 | 使用场景 |
|---|---|
| 疾控中心免疫规划科工作人员 | 将市级疾控下发的批签发合并 PDF 拆分存档 |
| 疾控信息管理员 | 维护批签发索引数据库，按批号检索证明文件 |

**用户特点：** 熟悉 Office 操作，不具备命令行使用能力，以 Windows 为主，部分单位使用国产操作系统。

---

## 三、支持平台

| 平台 | 版本要求 | 安装包格式 | 备注 |
|---|---|---|---|
| Windows | Windows 10 / 11（64位） | `.exe` 安装程序 | 优先级最高，用户最多 |
| macOS | macOS 12 Monterey 及以上 | `.dmg` 磁盘映像 | Apple Silicon / Intel 双架构 |
| 麒麟 | Kylin V10（aarch64 / x86_64） | `.deb` / `.rpm` | 政务网常见国产系统 |
| UOS | UOS 20 及以上（aarch64 / x86_64） | `.deb` | 与麒麟共用大部分构建流程 |

---

## 四、核心功能

### 4.1 主流程

```
上传大 PDF
    ↓
自动识别首页（二维码优先 → OCR 兜底）
    ↓
浏览器内展示识别结果，支持在线编辑
    ↓
一键拆分，下载 ZIP 压缩包
    ↓
（可选）写入本地批签发索引数据库
```

### 4.2 功能清单

| 功能 | 说明 | 优先级 |
|---|---|---|
| PDF 上传 | 拖拽或点击选择，支持大文件 | P0 |
| 二维码识别 | 扫描首页 QR，调用国家药监局 API 获取准确字段 | P0 |
| OCR 兜底识别 | QR 失败时自动 OCR，支持扫描版 PDF | P0 |
| 结果在线编辑 | 批号、页码、疫苗名称等可在页面直接修改 | P0 |
| 一键拆分下载 | 生成各单证 PDF，打包为 ZIP | P0 |
| 离线模式 | 无网络时禁用 QR，纯 OCR 运行 | P1 |
| 历史记录 | 本地 SQLite 保存已处理记录，支持按批号检索 | P1 |
| 导出 Excel 索引 | 拆分结果导出为 Excel，便于存档上报 | P1 |
| 批量处理 | 一次上传多个 PDF 依次处理 | P2 |

---

## 五、技术架构

### 5.1 整体结构

```
┌─────────────────────────────────────────┐
│           用户浏览器（前端）              │
│   HTML + CSS + 原生 JS（无外部框架）     │
└──────────────┬──────────────────────────┘
               │ HTTP（本地 localhost）
┌──────────────▼──────────────────────────┐
│          Flask 本地 Web 服务器           │
│   detect / split / list 逻辑复用        │
│   现有 batch_cert_splitter.py 核心不变  │
└──────────────┬──────────────────────────┘
               │
       ┌───────┴────────┐
       │                │
  SQLite 数据库      文件系统
  (cert.db)       (output/, logs/)
```

### 5.2 关键技术选型

| 组件 | 技术方案 | 说明 |
|---|---|---|
| Web 框架 | Flask | 轻量，打包体积小 |
| 前端 | 原生 HTML/CSS/JS | 无 Node.js 依赖，便于打包 |
| OCR（macOS） | macOS Vision 框架 | 无需额外模型 |
| OCR（Win/麒麟/UOS） | Tesseract + pytesseract | 语言包随安装包分发 |
| QR 解码 | OpenCV | 跨平台，已验证可用 |
| 打包 | PyInstaller | 生成单目录可执行文件 |
| 安装包（Windows） | Inno Setup | 生成标准 `.exe` 安装程序 |
| 安装包（macOS） | create-dmg | 生成带拖拽安装的 `.dmg` |
| 安装包（麒麟/UOS） | fpm | 生成 `.deb` / `.rpm` |
| CI/CD | GitHub Actions | 四平台自动构建 + 发布 |

### 5.3 目录结构（规划）

```
batch_cert_splitter/
├── tools/
│   └── batch_cert_splitter.py    现有核心逻辑（保持不变）
├── app/
│   ├── server.py                 Flask 入口
│   ├── routes/
│   │   ├── detect.py             识别接口
│   │   ├── split.py              拆分接口
│   │   └── records.py            历史记录接口
│   └── static/
│       ├── index.html            主页面
│       ├── style.css
│       └── app.js
├── build/
│   ├── windows/                  Inno Setup 脚本
│   ├── macos/                    create-dmg 配置
│   └── linux/                    fpm 打包脚本
├── .github/
│   └── workflows/
│       └── release.yml           四平台自动构建
├── tools/                        现有
├── 产品规划.md                   本文件
└── 使用说明.md                   现有（随产品更新）
```

---

## 六、页面设计

### 6.1 主页面布局

```
┌──────────────────────────────────────────────┐
│  批签发证明拆分工具          [历史记录] [设置] │
├──────────────────────────────────────────────┤
│                                              │
│   ┌──────────────────────────────────────┐  │
│   │                                      │  │
│   │      拖拽 PDF 文件到此处上传          │  │
│   │      或点击选择文件                   │  │
│   │                                      │  │
│   └──────────────────────────────────────┘  │
│                                              │
│  识别结果（可直接修改）：                     │
│  ┌──────┬──────────┬────┬────┬──────┬────┐  │
│  │ 批号 │ 疫苗名称 │起页│止页│批签号│置信│  │
│  ├──────┼──────────┼────┼────┼──────┼────┤  │
│  │ 可编辑表格行                           │  │
│  └──────────────────────────────────────┘  │
│                                              │
│  [+ 添加行]              [确认并拆分 ▶]      │
└──────────────────────────────────────────────┘
```

### 6.2 交互流程

1. 拖入 PDF → 显示进度条（识别中）
2. 识别完成 → 展示结果表格，QR命中行显示绿色，OCR行黄色，未识别行红色
3. 用户在表格内直接点击编辑（尤其是红色行）
4. 点击"确认并拆分" → 弹出进度 → 完成后自动下载 ZIP

---

## 七、分发与发布

### 7.1 官方发布渠道

| 渠道 | 用途 |
|---|---|
| GitHub Pages | 产品介绍页（截图、功能说明、下载入口） |
| GitHub Releases | 各平台安装包托管（自动 CI 发布） |
| 国内镜像（Gitee / 备用） | 解决部分网络访问问题 |

### 7.2 版本命名

`v主版本.次版本.修订` —— 示例：`v1.0.0`

- 主版本：功能重大变更
- 次版本：新增功能
- 修订：Bug 修复

### 7.3 GitHub Releases 文件规划

```
v1.0.0/
├── 批签发拆分工具_v1.0.0_Windows_x64.exe
├── 批签发拆分工具_v1.0.0_macOS_arm64.dmg
├── 批签发拆分工具_v1.0.0_macOS_x64.dmg
├── 批签发拆分工具_v1.0.0_Kylin_aarch64.deb
├── 批签发拆分工具_v1.0.0_Kylin_x86_64.deb
├── 批签发拆分工具_v1.0.0_UOS_aarch64.deb
└── SHA256SUMS.txt
```

---

## 八、开发里程碑

| 阶段 | 内容 | 目标 |
|---|---|---|
| **M1 Web UI** | Flask 服务 + 主页面 + 上传识别 + 结果编辑 + 拆分下载 | 功能完整可用 |
| **M2 打包** | Windows `.exe` + macOS `.dmg`，双平台可运行 | 可分发给用户测试 |
| **M3 国产系统** | 麒麟 + UOS `.deb` 打包，Tesseract 随包分发 | 四平台覆盖 |
| **M4 发布页** | GitHub Pages 介绍页 + CI/CD 自动构建发布 | 正式上线 |
| **M5 完善** | 历史记录、Excel 导出、批量处理、自动更新检查 | 体验提升 |

---

## 九、已完成基础

当前命令行版本（`tools/batch_cert_splitter.py`）已实现：

- [x] 二维码识别 + 国家药监局 API 精确提取字段
- [x] macOS Vision OCR / Tesseract OCR 跨平台兜底
- [x] 批号正则提取（含括号后缀、多种版式）
- [x] PDF 拆分 + SQLite 入库
- [x] Excel 复核表生成与读取
- [x] 质控检查（页码重叠、遗漏、异常页数）
- [x] 基于 21 个真实 PDF（141 份证明）的测试验证

M1 的核心工作是将上述逻辑包装为 Web 接口，现有代码**无需重写**。
