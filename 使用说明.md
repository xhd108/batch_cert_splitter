# 批签发大 PDF 拆分工具 — 使用说明

## 环境依赖

```bash
pip install pymupdf openpyxl
```

---

## 使用流程

### 第一步：初始化数据库（首次使用执行一次）

```bash
python3 tools/batch_cert_splitter.py init
```

在 `db/cert.db` 创建 SQLite 数据库，包含以下两张表：

- `batch_files`：记录每次导入的源 PDF 文件
- `cert_index`：记录每个批号对应的单证信息及拆分结果

---

### 第二步：生成 Excel 索引模板

```bash
python3 tools/batch_cert_splitter.py template
```

在当前目录生成 `批签发索引模板.xlsx`，也可指定文件名：

```bash
python3 tools/batch_cert_splitter.py template --out 2025年5月索引.xlsx
```

---

### 第三步：人工填写索引表

用 Excel 打开模板，从第 3 行起每行填写一个批号：

| 列 | 字段 | 是否必填 | 说明 |
|---|---|---|---|
| A | 批号 | ✅ 必填 | 与疫苗标签批号一致 |
| B | 疫苗名称 | 建议填 | 如：冻干人用狂犬病疫苗 |
| C | 生产企业 | 建议填 | 如：长春卡介苗研究所 |
| D | 批签发号 | 建议填 | 如：2025S00123 |
| E | 起始页 | ✅ 必填 | 该证明在大 PDF 中的首页页码（从 1 起） |
| F | 结束页 | ✅ 必填 | 该证明在大 PDF 中的末页页码（含末页） |
| G | 备注 | 选填 | 如需标注特殊情况 |

> 第 1 行为列名，勿删勿改；第 2 行为填写说明，可删除。

---

### 第四步：拆分 PDF 并入库

```bash
python3 tools/batch_cert_splitter.py split --pdf 批签发合并.pdf --index 填好的索引表.xlsx
```

可选指定输出目录（默认为 `output/<PDF文件名>/`）：

```bash
python3 tools/batch_cert_splitter.py split \
  --pdf 批签发合并.pdf \
  --index 填好的索引表.xlsx \
  --out-dir output/2025年5月/
```

程序将：

1. 验证索引表（页码越界、重叠、空缺均会提示）
2. 按起止页提取每个批号对应页面，保存为独立 PDF
3. 将源文件信息和单证记录写入 SQLite 数据库
4. 在终端输出拆分报告

输出文件命名规则：`{批号}_{批签发号}.pdf`（无批签发号时为 `{批号}.pdf`）

---

### 第五步：查看入库记录

```bash
python3 tools/batch_cert_splitter.py list
```

默认显示最近 50 条，可调整：

```bash
python3 tools/batch_cert_splitter.py list --limit 100
```

---

## 质控说明

| 情况 | 处理方式 |
|---|---|
| 起始页 > 结束页 | 报错，终止拆分 |
| 页码超出 PDF 总页数 | 报错，终止拆分 |
| 页码范围与其他批号重叠 | 报错，终止拆分 |
| 某页未被任何批号覆盖 | 警告提示，继续拆分 |
| 单证页数不在 2-3 页 | 拆分成功，标记为"待复核"，报告中显示 ⚠ |

> 报错终止时，数据库不写入任何记录，修正索引表后可重新运行。

---

## 目录结构

```
batch_cert_splitter/
├── tools/
│   └── batch_cert_splitter.py   主工具
├── db/
│   └── cert.db                  SQLite 数据库（自动创建）
├── output/
│   └── <PDF文件名>/             拆分结果（自动创建）
├── logs/                        运行日志（自动创建）
├── readme.md                    项目背景说明
└── 使用说明.md                  本文件
```
