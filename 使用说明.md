# 批签发大 PDF 拆分工具 — 使用说明

## 环境依赖

### macOS

```bash
pip3 install pymupdf openpyxl opencv-python pyobjc-framework-Vision pyobjc-framework-Quartz
```

> - `opencv-python`：二维码解码（优先使用，直接从国家药监局获取准确数据）
> - `pyobjc-framework-Vision`：OCR 兜底引擎，**无需额外下载模型**

### 麒麟 / Linux

```bash
# 安装 Tesseract 及中文语言包
sudo apt install tesseract-ocr tesseract-ocr-chi-sim tesseract-ocr-chi-tra

# 安装 Python 依赖
pip3 install pymupdf openpyxl opencv-python-headless pytesseract
```

> 程序自动检测操作系统，优先使用二维码，OCR 作为兜底。

---

## 两种工作流程

| 流程 | 适用场景 | 步骤 |
|---|---|---|
| **v2 自动流程**（推荐） | 页数多、扫描版 PDF | detect → 人工复核 → split |
| **v1 手动流程** | 页数少、人工核对方便 | template → 手动填写 → split |

---

## v2 自动流程（推荐）

### 第一步：自动识别首页，生成复核表

```bash
python3 tools/batch_cert_splitter.py detect --pdf 批签发合并.pdf
```

- 程序自动判断是否为扫描件，扫描件自动启用 OCR
- 生成 `批签发合并_复核表.xlsx`，包含自动识别的批号、疫苗名称、生产企业、批签发号、起止页
- 置信度颜色标注：🟢 高置信（可靠）/ 🟡 中置信（建议核查）/ 🔴 手动（需填写）

**文件名含空格时需加引号：**

```bash
python3 tools/batch_cert_splitter.py detect --pdf "2026年二季度批签发 2026-04-02 .pdf"
```

扫描不清晰、出现大页数区间时，提升 OCR 分辨率：

```bash
python3 tools/batch_cert_splitter.py detect --pdf 批签发合并.pdf --dpi 300
```

离线环境（无法联网）时，禁用二维码模式，仅使用 OCR：

```bash
python3 tools/batch_cert_splitter.py detect --pdf 批签发合并.pdf --no-qr
```

### 第二步：人工复核 Excel

用 Excel 打开复核表，重点核查：

- 🟢 高置信行：确认批号、疫苗名称是否正确（通常无需修改）
- 🟡 中置信行：核查起止页和字段是否有误
- 🔴 红色行：手动填写批号等缺失信息
- 若某份证明起止页有误，直接修改"起始页"和"结束页"列

> 第 1 行为列名，勿删勿改；"置信度"和"备注"列不影响拆分，仅供参考。

**常见需人工修正的情况：**

| 现象 | 原因 | 处理方式 |
|---|---|---|
| 批号为空（🔴 行） | OCR 质量差，批号区域乱码 | 手动填写批号 |
| 某份证明页数 ≥ 5 | 漏检了中间的首页 | 拆成多行，各自填写起止页 |
| PDF 前几页为生产许可证等附件 | 非批签发证明页面 | 删除对应行 |
| 批号后有括号如 `202506022（1-2）` | 表示同批多组，属正常批号格式 | 无需修改，直接使用 |

### 第三步：拆分并入库

```bash
python3 tools/batch_cert_splitter.py split \
  --pdf 批签发合并.pdf \
  --index 批签发合并_复核表.xlsx
```

---

## v1 手动流程

### 第一步：初始化数据库（首次使用执行一次）

```bash
python3 tools/batch_cert_splitter.py init
```

### 第二步：生成 Excel 索引模板

```bash
python3 tools/batch_cert_splitter.py template
```

### 第三步：人工填写索引表

用 Excel 打开 `批签发索引模板.xlsx`，从第 3 行起每行填写一个批号：

| 列 | 字段 | 是否必填 | 说明 |
|---|---|---|---|
| A | 批号 | ✅ | 与疫苗标签批号一致 |
| B | 疫苗名称 | 建议填 | 如：冻干人用狂犬病疫苗 |
| C | 生产企业 | 建议填 | 如：长春卡介苗研究所 |
| D | 批签发号 | 建议填 | 如：LRA20260923 |
| E | 起始页 | ✅ | 该证明在大 PDF 中的首页页码（从 1 起） |
| F | 结束页 | ✅ | 该证明在大 PDF 中的末页页码（含末页） |
| G | 备注 | 选填 | |

### 第四步：拆分并入库

```bash
python3 tools/batch_cert_splitter.py split --pdf 批签发合并.pdf --index 填好的索引表.xlsx
```

---

## 通用操作

### 查看已入库记录

```bash
python3 tools/batch_cert_splitter.py list
python3 tools/batch_cert_splitter.py list --limit 100
```

### 指定输出目录

```bash
python3 tools/batch_cert_splitter.py split \
  --pdf 批签发合并.pdf \
  --index 复核表.xlsx \
  --out-dir output/2026年5月/
```

---

## 质控说明

| 情况 | 处理方式 |
|---|---|
| 起始页 > 结束页 | 报错，终止拆分 |
| 页码超出 PDF 总页数 | 报错，终止拆分 |
| 页码范围与其他批号重叠 | 报错，终止拆分 |
| 某页未被任何批号覆盖 | 警告提示，继续拆分 |
| 单证页数不在 2-4 页 | 拆分成功，标记"待复核"，报告中显示 ⚠ |

> 报错终止时，数据库不写入任何记录，修正索引/复核表后可重新运行。

---

## 自动识别字段说明

`detect` 命令从 OCR 文字中自动提取以下字段：

| 字段 | 识别逻辑 |
|---|---|
| 批号 | `Lot No.` 下一行；或批号值出现在 `Lot No.` 标签之前（OCR 列读序）；或出现在"收检编号"之前；支持括号后缀如 `202506022（1-2）` |
| 批签发号 | 优先识别国际格式 `LR[A-Z]+数字`（如 LRA20260923）；回退识别中文格式（如批签中检20260923） |
| 疫苗名称 | `Generic Name` 下一行；或含"疫苗"的短行 |
| 生产企业 | `Manufacturer` 下一行；或"生产企业："后的内容 |

**识别准确率（基于21个真实PDF、141份证明测试）：**

- 首页检测：约 95% 高置信自动识别
- 二维码命中时：批号、批签发号、疫苗名称、生产企业**100% 准确**（数据来源：国家药监局）
- OCR 兜底时：批号提取约 85%（剩余 15% 因扫描乱码需人工填写）
- 假阳性（误识为首页）：极少，企业内部报告页自动排除

**识别优先级：** 二维码 → OCR → 人工填写

---

## 目录结构

```
batch_cert_splitter/
├── tools/
│   └── batch_cert_splitter.py   主工具（v2，含 OCR）
├── 批签发/                       原始 PDF 存放目录（示例）
├── db/
│   └── cert.db                  SQLite 数据库（自动创建）
├── output/
│   └── <PDF文件名>/             拆分结果（自动创建）
├── logs/                        运行日志（自动创建）
├── readme.md                    项目背景与版本规划
└── 使用说明.md                  本文件
```
